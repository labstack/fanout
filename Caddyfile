# Caddy at the edge of the Compose project. Uses ACME DNS-01 via
# Cloudflare (see Dockerfile.caddy for the rationale and plugin). CF_API_TOKEN
# is a single scoped Cloudflare API token with `Zone.Zone:Read` +
# `Zone.DNS:Edit` on the `fanout.run` and `labstack.com` zones — provided
# via .env on the deploy host. `fanout-site`, `fanout-demo`, and `fanout`
# resolve over the Compose default bridge DNS.

{
	email {env.LETSENCRYPT_EMAIL}
	acme_dns cloudflare {env.CF_API_TOKEN}
}

# Wildcard cert covers any *.fanout.run subdomain. Adding a new subdomain
# means a `@name host name.fanout.run` matcher + a `handle @name { reverse_proxy … }`
# block inside this site block — no new cert needed. The `handle` wrapper
# is REQUIRED (see fallback-block comment below); a bare `reverse_proxy @name`
# would re-introduce the routing bug PR #117 fixed.
# Canonical pattern: https://caddyserver.com/docs/caddyfile/patterns#wildcard-certificates
*.fanout.run {
	# Wrap each subdomain route in `handle @name { … }`. Do NOT unwrap them.
	@demo host demo.fanout.run
	handle @demo {
		reverse_proxy fanout-demo:7520
	}

	# Fallback for not-yet-configured subdomains. `handle` blocks are mutually
	# exclusive at the same nesting level — only the first matching one runs.
	# That mutual exclusion is what keeps this `respond` from firing on
	# `demo.fanout.run`: without `handle`, both `reverse_proxy @demo` and a
	# bare `respond` would coexist in Caddy's single auto-ordered middleware
	# chain, and `respond` (ordered ahead of `reverse_proxy`) would win.
	# 404 (not abort) keeps the failure debuggable — a clear HTTP response
	# beats a connection reset that looks like an outage when troubleshooting DNS.
	handle {
		respond "fanout.run: subdomain not configured" 404
	}

	encode gzip zstd
}

# Wildcards don't cover the apex (WebPKI restriction), so the apex needs
# its own site block. `www.fanout.run` is intentionally routed here too so
# it shares the apex's marketing-site backend instead of falling into the
# wildcard's 404.
fanout.run, www.fanout.run {
	reverse_proxy fanout-site:80
	encode gzip zstd
}

fanout.labstack.com {
	reverse_proxy fanout:7520
	encode gzip zstd
}

# OTLP/gRPC ingest on the standard port, reusing the cert Caddy manages
# for the host on :443 (TLS doesn't care about port). h2c (HTTP/2 cleartext)
# upstream is required for gRPC. Clients:
#   OTEL_EXPORTER_OTLP_ENDPOINT=https://fanout.labstack.com:4317
#   OTEL_EXPORTER_OTLP_PROTOCOL=grpc
#   OTEL_EXPORTER_OTLP_HEADERS=x-fanout-ingest-token=fo_...
fanout.labstack.com:4317 {
	reverse_proxy h2c://fanout:4317
}
