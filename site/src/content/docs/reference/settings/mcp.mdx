---
title: "MCP settings"
description: "Whether the MCP server is served, and the public resource URI it binds tokens to."
summary: "Settings for the MCP endpoint external agents connect to."
read_when:
  - "You are connecting an external agent to a Fanout instance."
generated: true
---

{/* Generated by cmd/fanout-docgen from internal/config. Edit the generator, not this page. */}

Every setting below is read from the environment variable in the second
column, or from the same key in a YAML configuration file. An unrecognised
`FANOUT_`-prefixed variable is a startup error, so a renamed setting surfaces
as a refusal to start rather than as a default nobody chose.

| Setting | Environment variable | Type | Default |
|---|---|---|---|
| `mcp.enabled` | `FANOUT_MCP_ENABLED` | boolean | `true` |
| `mcp.public_url` | `FANOUT_MCP_PUBLIC_URL` | string | `https://localhost:7520/mcp` |

## Notes

### `mcp.public_url`

The canonical externally reachable MCP resource URI used for OAuth discovery and token audience binding. It must be stable across restarts and include the /mcp path.
