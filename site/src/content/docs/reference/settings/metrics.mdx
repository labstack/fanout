---
title: "Metrics settings"
description: "The Prometheus endpoint's token and whether it is public."
summary: "Settings for the Prometheus scrape endpoint at /-/metrics."
read_when:
  - "You are scraping Fanout's own metrics."
generated: true
---

{/* Generated by cmd/fanout-docgen from internal/config. Edit the generator, not this page. */}

Every setting below is read from the environment variable in the second
column, or from the same key in a YAML configuration file. An unrecognised
`FANOUT_`-prefixed variable is a startup error, so a renamed setting surfaces
as a refusal to start rather than as a default nobody chose.

| Setting | Environment variable | Type | Default |
|---|---|---|---|
| `metrics.public` | `FANOUT_METRICS_PUBLIC` | boolean | `false` |
| `metrics.token` | `FANOUT_METRICS_TOKEN` | string · secret | — |

:::caution[Secrets]
Settings marked `secret` are redacted from Fanout's startup configuration
log. They are still plain environment variables: anything that can read the
process environment can read them.
:::
