---
/* The end of the page.
 *
 * A landing page that stops after its last argument leaves the reader to
 * scroll back up for the button. This restates the one action and shows the
 * command, because for a single binary the command *is* the product demo —
 * there is nothing to sign up for and nothing to provision.
 *
 * The command is the one from the install page, in the form that verifies its
 * download. Keep the two in step; a copyable command that has drifted is worse
 * than no command.
 */
---

<div class="fo-cta">
  <h2>Install it and point something at it.</h2>
  <p>
    One binary, one data directory. The installer checks the archive against the
    release checksums before it extracts anything.
  </p>
  <pre><code>curl -fsSL https://raw.githubusercontent.com/labstack/fanout/main/scripts/install.sh | sh</code></pre>
  <div class="fo-cta-actions">
    <a class="fo-cta-primary" href="/start/install">Read the install guide</a>
    <a class="fo-cta-ghost" href="https://demo.fanout.run">See the live demo</a>
  </div>
</div>

<style>
  .fo-cta {
    margin-top: clamp(3.5rem, 7vw, 6rem);
    padding: clamp(2rem, 4vw, 3rem);
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 12px;
    background: var(--sl-color-gray-6);
  }

  .fo-cta h2 {
    font-family: var(--fo-font-display);
    font-size: clamp(1.4rem, 2.4vw, 1.9rem);
    line-height: 1.2;
    letter-spacing: -0.015em;
    font-weight: 500;
    margin: 0;
    color: var(--sl-color-white);
    overflow-wrap: normal;
    text-wrap: balance;
  }

  .fo-cta p {
    font-family: var(--fo-font-body);
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--sl-color-gray-2);
    margin: 0.85rem 0 0;
    max-width: 60ch;
  }

  /* The command scrolls inside its own box rather than widening the page — the
     same rule the reference tables follow, and it is focusable for the same
     reason: an overflow a keyboard cannot reach cannot be read. */
  .fo-cta pre {
    margin: 1.5rem 0 0;
    padding: 0.85rem 1rem;
    overflow-x: auto;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 8px;
    background: var(--sl-color-black);
  }

  .fo-cta pre:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 2px;
  }

  .fo-cta code {
    font-family: var(--fo-font-display);
    font-size: 0.8rem;
    color: var(--sl-color-gray-2);
    white-space: pre;
  }

  .fo-cta-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem;
    margin-top: 1.5rem;
  }

  .fo-cta-primary,
  .fo-cta-ghost {
    display: inline-block;
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 7px;
    padding: 0.7rem 1.15rem;
    font-family: var(--fo-font-display);
    font-size: 0.88rem;
    font-weight: 600;
    text-decoration: none;
    color: var(--sl-color-white);
  }

  .fo-cta-primary {
    background: var(--sl-color-white);
    border-color: var(--sl-color-white);
    color: var(--sl-color-black);
  }

  .fo-cta-primary:hover {
    background: var(--sl-color-gray-1);
    border-color: var(--sl-color-gray-1);
  }

  .fo-cta-ghost:hover {
    border-color: var(--sl-color-gray-4);
    background: var(--sl-color-black);
  }

  .fo-cta-primary:focus-visible,
  .fo-cta-ghost:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 2px;
  }
</style>
